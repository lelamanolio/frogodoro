{
  "name": "frogodoro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "out/main/main.js",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint . --fix --cache",
    "format": "prettier --write --experimental-cli src/",
    "electron-dev": "electron-vite dev",
    "electron-build": "electron-vite build",
    "electron-build:win": "electron-vite build && electron-builder --win",
    "electron-build:mac": "electron-vite build && electron-builder --mac",
    "electron-build:linux": "electron-vite build && electron-builder --linux",
    "electron-build:all": "electron-vite build && electron-builder --win --mac --linux"
  },
  "dependencies": {
    "vue": "^3.5.26"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@vitejs/plugin-vue": "^6.0.3",
    "@vue/eslint-config-prettier": "^10.2.0",
    "electron": "^40.4.1",
    "electron-builder": "^26.7.0",
    "electron-vite": "^5.0.0",
    "eslint": "^9.39.2",
    "eslint-plugin-vue": "~10.6.2",
    "globals": "^17.0.0",
    "prettier": "3.7.4",
    "sass-embedded": "^1.97.2",
    "vite": "^7.3.0",
    "vite-plugin-vue-devtools": "^8.0.5"
  }
}
